---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "defectdojo_cicd_infrastructure Data Source - terraform-provider-defectdojo"
subcategory: ""
description: |-
  Data source for DefectDojo CI/CD Infrastructure. You can specify either the id or the name to look up the CI/CD infrastructure. A name is unique together with infrastructure_type, so looking up by name when more than one infrastructure_type shares that name will fail; use id instead in that case. DefectDojo hides description and url from a token without the view_cicdinfrastructure or change_cicdinfrastructure permission; both attributes then read as empty.
---

# defectdojo_cicd_infrastructure (Data Source)

Data source for DefectDojo CI/CD Infrastructure. You can specify either the `id` or the `name` to look up the CI/CD infrastructure. A name is unique together with `infrastructure_type`, so looking up by name when more than one infrastructure_type shares that name will fail; use `id` instead in that case. DefectDojo hides `description` and `url` from a token without the `view_cicdinfrastructure` or `change_cicdinfrastructure` permission; both attributes then read as empty.

## Example Usage

```terraform
# Lookup by name
data "defectdojo_cicd_infrastructure" "example" {
  name = "Jenkins"
}

# Or lookup by ID
# data "defectdojo_cicd_infrastructure" "by_id" {
#   id = "1"
# }
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) Identifier
- `name` (String) The name of the CI/CD infrastructure. Specify either id or name.

### Read-Only

- `description` (String) The description of the CI/CD infrastructure. DefectDojo hides this from a token without the `view_cicdinfrastructure` or `change_cicdinfrastructure` permission.
- `infrastructure_type` (String) The kind of CI/CD infrastructure. Valid values: `scm_server`, `build_server`, `orchestration`.
- `url` (String) The public URL of the CI/CD infrastructure. DefectDojo hides this from a token without the `view_cicdinfrastructure` or `change_cicdinfrastructure` permission.
